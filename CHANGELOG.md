# Changelog

All notable changes documented here.

## [Unreleased]

### Added
- Initial type scaffold: `RtmpIngressConfig`, `RtmpIngressBinding`, `VirtualDeviceSpec`, `VirtualDeviceHandle`
- `bindRtmpIngress(config)` factory (stub gateway URL)
- `registerVirtualDevice(spec)` factory (stub handle until per-platform companions ship)
- Per-platform driver design docs (macOS / Windows / Linux)
- Foundation chassis: CODEOWNERS, SECRETS.md, foundation-gate
